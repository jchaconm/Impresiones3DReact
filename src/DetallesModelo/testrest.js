export function listaPosts(type,userData){

 /*
 var BaseUrl='http://localhost:8085/http://localhost:9002/publicacion/listar';

 const xs=JSON.stringify({
      "pIdCanal": 0});


   
    const xs=JSON.stringify(userData) ;

    fetch(BaseUrl,{
        method:'POST',
        body:xs,
        mode:'cors',
        headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json'}
        

    })
    .then((response)=> response.json())
    .then((responseJson)=>{

    })
    .catch((error)=> {
      console.log(error);
    })


  var idUsuario = parseInt(1);

 var BaseUrl='http://localhost:8085/http://localhost:9002/publicacion/listar';

  return new Promise((resolve,reject) =>{
   
    const xs=JSON.stringify(userData) ;

    fetch(BaseUrl,{
        method:'POST',
        mode:'cors',
        body:JSON.stringify({pIdCanal:0}),
        headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json'}
        

    })
    .then((response)=> response.json())
    .then((responseJson)=>{
        resolve(responseJson);
              console.log(responseJson);

      




    })
    .catch((error)=> {
      console.log(error);
    })


  });
  */
  
}